#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
vector<ll>vec;
int main(){
    ll n,m,temp;
    while(true){
        cin>>n>>m;
        if(cin.fail())break;

        for(ll i=0;i<n;i++){
            vec.push_back(temp);
        }
    }
    
}